import 'package:forever_fusion/models/orders_model.dart';

class MailService {
  // The email feature has been removed.

  // If you need to keep this class for future use, you can add other notification methods here.
  
  void sendMailFromGmail(String receiver, OrdersModel order) {
    print("Email sending feature is disabled.");
  }
}
