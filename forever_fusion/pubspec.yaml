name: forever_fusion
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^3.12.1
  cloud_firestore: ^5.6.5
  firebase_auth: ^5.5.1
  carousel_slider: ^5.0.0
  badges: ^3.1.2
  http: ^1.3.0
  flutter_stripe: ^11.4.0
  flutter_dotenv: ^5.2.1
  mailer: ^6.4.1
  shimmer: ^3.0.0
  provider: ^6.1.2
  video_player: ^2.9.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/.env  # Added to load environment variables
